services:
  - type: web
    name: websocket-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node push-notification-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /

